# 07. 컴포넌트 설계서

| 항목 | 내용 |
|------|------|
| 프로젝트명 | CorpBrain |
| 문서 버전 | v1.1 |
| 작성일 | 2026-07-03 |

---

## 1. 컴포넌트 구조

```
src/
├── app/                    # 페이지 (App Router)
│   ├── page.tsx            # 채팅 메인
│   ├── login/page.tsx
│   ├── admin/page.tsx
│   └── guide/page.tsx
├── components/             # 재사용 UI
└── lib/                    # 비즈니스 로직
```

---

## 2. 페이지 컴포넌트

| 컴포넌트 | 파일 | 타입 | 설명 |
|----------|------|------|------|
| `Chat` | `app/page.tsx` | Client | 메인 채팅, useChat |
| `LoginPage` | `app/login/page.tsx` | Client | 로그인·시드 계정 |
| `AdminPage` | `app/admin/page.tsx` | Client | 대시보드 |
| `GuidePage` | `app/guide/page.tsx` | Server/Client | 매뉴얼 |

### Chat (`page.tsx`) 상태

| State | 타입 | 용도 |
|-------|------|------|
| `messages` | UIMessage[] | useChat |
| `input` | string | 입력창 |
| `isIndexing` | boolean | Sync Vault |
| `treeOpen` | boolean | 모바일 문서 트리 드로어 |

### Chat 의존성

- `@ai-sdk/react` `useChat` + `DefaultChatTransport`
- `next-auth/react` `useSession`
- `extractMessageText` — 메시지 텍스트 추출

---

## 3. 공통 컴포넌트

### 3.1 `ChatMessageContent`

| 항목 | 내용 |
|------|------|
| 파일 | `components/chat-message.tsx` |
| Props | `{ content: string }` |
| 역할 | react-markdown + GFM, `[출처: ...]` 클릭 가능 뱃지 → `DocumentPreviewModal` |

### 3.2 `DocumentPreviewModal`

| 항목 | 내용 |
|------|------|
| 파일 | `components/document-preview-modal.tsx` |
| Props | `fileName`, `onClose` |
| 역할 | GET `/api/documents/content` 로드, md 마크다운·pdf/docx 텍스트 표시 |

### 3.3 `DocumentTree`

| 항목 | 내용 |
|------|------|
| 파일 | `components/document-tree.tsx` |
| Props | `userRole`, `onSelectDocument`, `mobileOpen`, `onMobileClose` |
| 역할 | GET `/api/documents/tree`, 검색 필터(`tree-filter`), 폴더 펼침, 문서 클릭 → 질문 |

### 3.4 `ChatFeedback`

| 항목 | 내용 |
|------|------|
| 파일 | `components/chat-feedback.tsx` |
| Props | `messageId`, `query?`, `sources?` |
| 역할 | 👍/👎 → POST `/api/chat/feedback` |

### 3.5 `DocumentUpload`

| 항목 | 내용 |
|------|------|
| 파일 | `components/document-upload.tsx` |
| Props | `userRole` |
| 역할 | multipart 업로드, role 선택 |

### 3.6 `HelpPanel`

| 항목 | 내용 |
|------|------|
| 파일 | `components/help-panel.tsx` |
| Props | `open`, `onClose`, `userRole` |
| 역할 | 슬라이드 패널, 가이드 섹션 |

### 3.7 `OnboardingBanner`

| 항목 | 내용 |
|------|------|
| 파일 | `components/onboarding-banner.tsx` |
| Props | `userRole`, `userName`, `onOpenGuide` |
| 역할 | 최초 사용자 안내 |

### 3.8 `QuickStartPrompts`

| 항목 | 내용 |
|------|------|
| 파일 | `components/quick-start-prompts.tsx` |
| Props | `userRole`, `onSelect` |
| 역할 | Role별 예시 질문 버튼 |

### 3.9 `Providers`

| 항목 | 내용 |
|------|------|
| 파일 | `components/providers.tsx` |
| 역할 | `SessionProvider` 래핑 |

### 3.10 `LoginGuide`

| 항목 | 내용 |
|------|------|
| 파일 | `components/login-guide.tsx` |
| 역할 | 로그인 페이지 안내 카드 |

---

## 4. lib 모듈 (핵심)

| 모듈 | 주요 export | 책임 |
|------|-------------|------|
| `lib/chat/rag.ts` | `retrieveRagContext`, `streamRagResponse` | RAG 공통 |
| `lib/chat/messages.ts` | `toModelMessages`, `extractMessageText` | AI SDK 변환 |
| `lib/search/query-context.ts` | `buildSearchQuery` | 멀티턴 |
| `lib/search/reranker.ts` | `rerankCandidates` | 2차 정렬 |
| `lib/vector-store/` | `hybridSearch`, `getVectorStore` | 검색 |
| `lib/indexer/` | `runIndexing`, `runIncrementalSync` | 인덱싱 |
| `lib/vault/tree-filter.ts` | `filterVaultTree`, `countVaultFiles` | 트리 검색 |
| `lib/vault/get-document.ts` | `getVaultDocumentContent` | 원문 API·RBAC |
| `lib/rbac.ts` | `canAccessDocument`, `getAccessibleRoles` | 권한 |
| `lib/auth/guard.ts` | `requireAuth` | API 가드 |

---

## 5. 컴포넌트–API 매핑

| 컴포넌트 | API |
|----------|-----|
| ChatMessageContent | GET `/api/documents/content` (뱃지 클릭) |
| DocumentTree | GET `/api/documents/tree` |
| Chat | POST `/api/chat` |
| ChatFeedback | POST `/api/chat/feedback` |
| DocumentUpload | POST `/api/upload` |
| Chat (Sync) | POST `/api/index` |
| AdminPage | GET `/api/admin/*` |

---

## 6. 변경 이력

| 버전 | 일자 | 변경 내용 |
|------|------|-----------|
| v1.0 | 2026-07-02 | 최초 작성 |
| v1.1 | 2026-07-03 | DocumentTree·DocumentPreviewModal·원문 API 연동 |
