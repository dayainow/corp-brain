# 05. 화면 설계서 (UI 명세서)

| 항목 | 내용 |
|------|------|
| 프로젝트명 | CorpBrain |
| 문서 버전 | v1.2 |
| 작성일 | 2026-07-03 |

---

## 1. UI 설계 원칙

- **업무용 톤**: NovaPay 사내 도구에 맞는 간결한 레이아웃
- **다크 모드**: Tailwind `dark:` 클래스 지원
- **반응형**: `lg:` 사이드바 / 모바일 드로어, `safe-area`·`visualViewport` 입력 고정
- **접근성**: 아이콘 버튼 `aria-label`, 시맨틱 heading

---

## 2. SCR-001 로그인 (`/login`)

### 레이아웃

```
┌─────────────────────────────────┐
│         CorpBrain (h1)          │
│    NovaPay Internal KB          │
├─────────────────────────────────┤
│  [처음이신가요? 사용 안내]        │
│  이메일 input                    │
│  비밀번호 input                  │
│  [로그인]                        │
│  ─── 또는 ───                   │
│  [Google로 로그인] (env 설정 시) │
├─────────────────────────────────┤
│  초기 배포 계정 (원클릭 입력)     │
│  김준호 / 박수연 / 이민호 ...     │
└─────────────────────────────────┘
```

### UI 요소

| 요소 | 타입 | 설명 |
|------|------|------|
| 이메일 | `input` placeholder `name@novapay.kr` | Credentials |
| 비밀번호 | `input[type=password]` | — |
| 로그인 | `button` | NextAuth signIn |
| 시드 계정 | 버튼 목록 | 자동 입력 |

---

## 3. SCR-002 채팅 (`/`)

### 레이아웃

```
┌──────────┬───────────────────────────────────────────────┐
│ 사내 문서 │ CorpBrain │ NovaPay … │ [문서][도움말][Admin]… │
│ [검색…]  ├───────────────────────────────────────────────┤
│  └ 트리  │ [온보딩 배너]                                   │
│  (lg+)   │  [참고 문서 카드] + 말풍선, 출처 뱃지(청크 하이라이트)   │
│          │  [이어서 물어보기] 후속 질문 칩 (마지막 답변)            │
│          ├───────────────────────────────────────────────┤
│          │  [질문 입력창 — safe-area]            [전송]     │
└──────────┴───────────────────────────────────────────────┘
```

- **데스크톱 (`lg+`)**: 좌측 `DocumentTree` 사이드바 (폭 `w-72`)
- **모바일**: 헤더 `문서` 버튼 → 좌측 드로어 (`role=dialog`, Escape 닫기, 검색 포커스)

### 스트리밍·신뢰 UX

| 단계 | UI |
|------|-----|
| 검색 중 | `ChatStreamingStatus` — 「사내 문서를 검색하고 있습니다…」 |
| 생성 중 | 「답변을 생성하고 있습니다…」 |
| 참고 문서 | `CitationSourceCards` — 스트리밍 `data-rag-sources` |
| 완료 후 | `FollowUpChips` — 후속 질문 2~3개 |

### 헤더 액션 (Role별)

| 버튼 | Role | 동작 |
|------|------|------|
| 문서 | 전체 | 모바일: 트리 드로어 / 데스크톱: 사이드바 |
| 도움말 | 전체 | HelpPanel 슬라이드 |
| Admin | admin | `/admin` 이동 |
| Sync Vault | admin | POST `/api/index` |
| 업로드 | manager+ | DocumentUpload 모달 |
| Clear | 전체 | localStorage 세션 삭제 |
| 로그아웃 | 전체 | signOut |

### 채팅 메시지 스타일

| Role | 배경 | 정렬 |
|------|------|------|
| user | `bg-blue-600 text-white` | 우측 |
| assistant | `bg-white dark:bg-slate-800` + border | 좌측 |

### 출처 뱃지·원문 모달

- 패턴: `[출처: 파일명.md]`
- `ChatMessageContent`에서 **클릭 가능 뱃지** (`button`, `aria-label` 원문 보기)
- 클릭 시 `DocumentPreviewModal` — 제목·파일명·본문 + **RAG 청크 하이라이트** (`highlight-chunk.ts`)
- 닫기: `Escape`, 배경 클릭, X 버튼

### 문서 트리 검색 (사이드바)

| 요소 | 설명 |
|------|------|
| 검색 입력 | `type=search`, placeholder `문서 검색...` |
| 일치 건수 | `N건 일치` / `일치하는 문서 없음` |
| 필터 | 제목·파일명(확장자 제외) |
| 펼침 | 검색 중 매칭 폴더 자동 펼침 |
| 파일 행 | **보기** (`Eye`) → 원문 모달 · **질문** (`MessageSquare`) → 채팅 전송 |
| 모바일 드로어 | body 스크롤 잠금, `aria-modal`, 검색창 자동 포커스 |

---

## 4. SCR-003 가이드 (`/guide`)

- 제목: `CorpBrain 사용 매뉴얼`
- Role별 섹션 필터 (`getSectionsForRole`)
- 채팅으로 돌아가기 링크

---

## 5. SCR-004 Admin (`/admin`)

```
┌────────────────────────────────────────┐
│ Admin Dashboard │ [운영 가이드][채팅]  │
├────────────────────────────────────────┤
│ [문서 수] [청크 수] [Admin문서] [로그수] │
├────────────────────────────────────────┤
│ 검색 품질: Hit@3 | Hit@1 | MRR         │
├──────────────────┬─────────────────────┤
│ 최근 감사 로그    │ 문서 목록 + role 뱃지│
└──────────────────┴─────────────────────┘
```

### Role 뱃지 색상

| Role | 색상 |
|------|------|
| general | green |
| manager | yellow |
| admin | red |

---

## 6. 공통 컴포넌트

| 컴포넌트 | 용도 |
|----------|------|
| `ChatStreamingStatus` | RAG 검색·생성 단계 |
| `CitationSourceCards` | 스트리밍 참고 문서 카드 |
| `FollowUpChips` | 후속 질문 칩 |
| `HelpPanel` | 우측 슬라이드 도움말 |
| `OnboardingBanner` | 상단 1회성 안내 |
| `QuickStartPrompts` | 빈 채팅 시 예시 질문 |
| `DocumentUpload` | 파일·role 선택 업로드 |
| `ChatFeedback` | 👍/👎 |
| `DocumentTree` | 좌측 사내 문서 탐색·검색 |
| `DocumentPreviewModal` | 출처 원문 오버레이 |

---

## 7. 디자인 토큰 (Tailwind)

| 용도 | 클래스 |
|------|--------|
| Primary | `blue-600`, `blue-700` |
| 배경 | `slate-50` / `slate-950` (dark) |
| 카드 | `rounded-2xl`, `shadow-sm` |
| 본문 | `text-slate-800` / `text-slate-100` |

---

## 8. 변경 이력

| 버전 | 일자 | 변경 내용 |
|------|------|-----------|
| v1.0 | 2026-07-02 | 최초 작성 |
| v1.1 | 2026-07-03 | 문서 트리 사이드바·검색·출처 원문 모달 |
| v1.2 | 2026-07-03 | RAG 단계·참고 카드·후속 칩·트리 보기/질문·모바일 safe-area |
